import os
import shutil
from datetime import datetime
import load_settings

class FileHandler:
    def gather_files(src_dir): # gather_files() 함수는 src_dir을 인자로 받아서 src_dir에 있는 모든 파일을 찾아 새 디렉토리로 이동시키는 함수
        pass